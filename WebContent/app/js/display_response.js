// This js file is for displayServerResponse. From every controller of our project we must give to this function controller type and the data that we want to allert. After adding another controller
// we need to add another case for our switch!!! We can change default js allert to another method...

function displayResponseForUser (controller, data) {
	switch(controller) {
		case "ProjectContoller":
			
			//window.alert("You have successfully " + data);
	}
}